# wishlist/signals.py
# Note: Wishlist price_when_added updates are handled in
# backend/products/api_views.py ProductViewSet.update() and partial_update()
# methods to ensure we have access to the old price before the update.